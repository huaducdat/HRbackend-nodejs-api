info:
  name: login
  type: http

http:
  method: POST
  url: http://localhost:5001/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "nguyenvana@example.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​